<glass-card intensity="strong" glow="{{true}}" padding="48rpx 40rpx" radius="32rpx">
  <view class="tb-inner">
    <view class="tb-label">{{label}}</view>
    <view class="tb-value">{{balance}}</view>
    <view class="tb-caption">{{caption}}</view>
  </view>
</glass-card>
