.card {
  box-sizing: border-box;
  background: #ffffff;
  border-radius: 24rpx;
  position: relative;
  overflow: hidden;
}

.card--subtle {
  background: #fafafa;
}

.card--normal {
  background: #ffffff;
}

.card--strong {
  background: #ffffff;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
}
